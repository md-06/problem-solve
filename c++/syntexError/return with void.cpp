#include<iostream>
using namespace std;
void main()
{
	int a;
	a=10;// a is l-value and 10 is r-value;
	cout<<a;
    return;
//if we not write return type then by default it as int so during void we
// have to write return keywords only otherwise get error (int return type error)	 

}